import { TrimmedRequiredDirective } from './trimmed-required-directive';

describe('TrimmedRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimmedRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
